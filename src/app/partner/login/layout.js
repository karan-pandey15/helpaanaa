export const metadata = {
  title: 'Partner Login & Registration',
  description:
    'Register as a Helpaana partner. Explore service categories, benefits, KYC registration, and contact support.',
};

export default function PartnerLoginLayout({ children }) {
  return children;
}
